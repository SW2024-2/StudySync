<body id="goal-edit-page">
<h1>目標編集</h1>

<%= form_with model: @goal, local: true do |form| %>
  <div>
    <%= form.label :target_time, "目標時間 (分)" %>
    <%= form.number_field :target_time %>
  </div>

  <div>
    <%= form.submit "目標を更新する" %>
  </div>
<% end %>

  <%= link_to "キャンセル", reports_path %>
</body>