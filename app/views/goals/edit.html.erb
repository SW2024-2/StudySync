<h2>目標設定を編集</h2>

<%= form_with model: @goal, local: true do |form| %>
  <div>
    <%= form.label :target_time, "目標時間" %>
    <%= form.number_field :target_time, value: @goal.target_time %> 分
  </div>

  <div>
    <%= form.submit "目標を更新" %>
  </div>
<% end %>

<%= link_to "キャンセル", reports_path %>
